spring:
  application:
    name: manage

  datasource:
    url: jdbc:mysql://localhost:3306/manage
    username: root
    password: YOUR_MYSQL_PASSWORD

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true